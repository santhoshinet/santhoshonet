<%= form_for(@aim) do |f| %>
  <% if @aim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aim.errors.count, "error") %> prohibited this aim from being saved:</h2>

      <ul>
      <% @aim.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
